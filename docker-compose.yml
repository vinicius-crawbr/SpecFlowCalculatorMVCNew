version: '3'
networks:
  web:
services:
  xunit-test:
    image: xunit:latest
    ports:
      - "5001:80"
    networks:
      - web
  # jenkins:
  #   image: jenkins-asp
  #   ports:
  #     - "8080:8080"
  #   networks:
  #     - web

